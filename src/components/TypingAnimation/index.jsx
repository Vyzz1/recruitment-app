import React from "react";
// import TypeAnimation from "react-type-animation";

const TypingAnimation = ({ strings }) => {
  return (
    // <TypeAnimation
    //   sequence={strings.reduce((acc, str) => [...acc, str, 2000], [])}
    //   wrapper="div"
    //   cursor={true}
    //   repeat={Infinity}
    //   style={{ fontSize: "24px" }}
    // />
    <div>Helo</div>
  );
};

export default TypingAnimation;
